<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lottery.infrastructure.persistent.dao.ActivityOrderMapper">

    <resultMap id="BaseResultMap" type="com.lottery.infrastructure.persistent.po.ActivityOrder">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="userId" column="user_id" jdbcType="VARCHAR"/>
            <result property="activityId" column="activity_id" jdbcType="BIGINT"/>
            <result property="activityName" column="activity_name" jdbcType="VARCHAR"/>
            <result property="strategyId" column="strategy_id" jdbcType="BIGINT"/>
            <result property="orderId" column="order_id" jdbcType="VARCHAR"/>
            <result property="orderTime" column="order_time" jdbcType="TIMESTAMP"/>
            <result property="state" column="state" jdbcType="TINYINT"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <insert id="myinsert" parameterType="com.lottery.infrastructure.persistent.dao.ActivityOrderMapper">
            insert into activity_order(user_id, activity_id, activity_name, strategy_id, order_id, order_time, state, create_time, update_time)
            values(#{userId}, #{activityId}, #{activityName}, #{strategyId}, #{orderId}, #{orderTime}, #{state}, now(), now())
    </insert>

    <select id="queryRaffleActivityOrderByUserId" resultType="com.lottery.infrastructure.persistent.po.ActivityOrder">
        select user_id, activity_id, activity_name, strategy_id, order_id, order_time, state
        from activity_order
        where user_id = #{userId}
    </select>
</mapper>
